<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookstore.mapper.WalletMapper">
    
    <!-- 结果映射 -->
    <resultMap id="WalletResultMap" type="com.bookstore.entity.Wallet">
        <id property="id" column="id"/>
        <result property="balance" column="balance"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <!-- 用户关联 -->
        <association property="user" javaType="com.bookstore.entity.User">
            <id property="id" column="user_id"/>
            <result property="username" column="username"/>
        </association>
    </resultMap>
    
    <!-- 根据ID查询钱包 -->
    <select id="findById" resultMap="WalletResultMap" parameterType="long">
        SELECT w.*, u.username
        FROM wallet w
        LEFT JOIN users u ON w.user_id = u.id
        WHERE w.id = #{id}
    </select>
    
    <!-- 根据用户ID查询钱包 -->
    <select id="findByUserId" resultMap="WalletResultMap" parameterType="long">
        SELECT w.*, u.username
        FROM wallet w
        LEFT JOIN users u ON w.user_id = u.id
        WHERE w.user_id = #{userId}
    </select>
    
    <!-- 保存钱包 -->
    <insert id="save" parameterType="com.bookstore.entity.Wallet" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO wallet (user_id, balance, create_time, update_time)
        VALUES (#{user.id}, #{balance}, #{createTime}, #{updateTime})
    </insert>
    
    <!-- 更新钱包 -->
    <update id="update" parameterType="com.bookstore.entity.Wallet">
        UPDATE wallet
        SET balance = #{balance}, update_time = NOW()
        WHERE id = #{id}
    </update>
    
    <!-- 更新钱包余额 -->
    <update id="updateBalance">
        UPDATE wallet
        SET balance = balance + #{amount}, update_time = NOW()
        WHERE id = #{id}
    </update>
    
    <!-- 删除钱包 -->
    <delete id="deleteById" parameterType="long">
        DELETE FROM wallet WHERE id = #{id}
    </delete>
</mapper>
