<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookstore.mapper.UserMapper">

    <!-- 结果映射 -->
    <resultMap id="UserResultMap" type="com.bookstore.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="address" column="address"/>
        <result property="role" column="role"/>
        <result property="balance" column="balance"/>
    </resultMap>

    <!-- 查询所有用户 -->
    <select id="findAll" resultMap="UserResultMap">
        SELECT * FROM users
    </select>

    <!-- 分页查询所有用户 -->
    <select id="findAllPaged" resultMap="UserResultMap">
        SELECT * FROM users
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 获取用户总数 -->
    <select id="count" resultType="int">
        SELECT COUNT(*) FROM users
    </select>

    <!-- 根据ID查询用户 -->
    <select id="findById" resultMap="UserResultMap" parameterType="long">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <!-- 根据用户名查询用户 -->
    <select id="findByUsername" resultMap="UserResultMap" parameterType="string">
        SELECT * FROM users WHERE username = #{username} LIMIT 1
    </select>

    <!-- 检查用户名是否存在 -->
    <select id="existsByUsername" resultType="int" parameterType="string">
        SELECT COUNT(*) FROM users WHERE username = #{username}
    </select>

    <!-- 保存用户 -->
    <insert id="save" parameterType="com.bookstore.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, email, phone, address, role, balance)
        VALUES (#{username}, #{password}, #{email}, #{phone}, #{address}, #{role}, #{balance})
    </insert>

    <!-- 更新用户 -->
    <update id="update" parameterType="com.bookstore.entity.User">
        UPDATE users
        SET username = #{username},
            password = #{password},
            email = #{email},
            phone = #{phone},
            address = #{address},
            role = #{role},
            balance = #{balance}
        WHERE id = #{id}
    </update>

    <!-- 删除用户 -->
    <delete id="deleteById" parameterType="long">
        DELETE FROM users WHERE id = #{id}
    </delete>

    <!-- 更新用户余额 -->
    <update id="updateBalance">
        UPDATE users
        SET balance = balance + #{amount}
        WHERE id = #{userId}
    </update>
</mapper>
